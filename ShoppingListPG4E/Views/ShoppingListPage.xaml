<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ShoppingListPG4E.Views.ShoppingListPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:ShoppingListPG4E.ViewModels"
    Title="Lista do sklepu">

    <ContentPage.BindingContext>
        <viewModels:ShoppingListViewModel />
    </ContentPage.BindingContext>

    <CollectionView ItemsSource="{Binding Items}" SelectionMode="None">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="15,10" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">

                    <CheckBox Grid.Column="0" 
                      IsChecked="{Binding Purchased, Mode=TwoWay}"
                      VerticalOptions="Center" />

                    <VerticalStackLayout Grid.Column="1" 
                                 VerticalOptions="Center" 
                                 Spacing="2">
                        <Label Text="{Binding Name}"
                       FontAttributes="Bold"
                       FontSize="16"
                       LineBreakMode="TailTruncation" />

                        <Label Text="{Binding Category}"
                       FontSize="12"
                       TextColor="{DynamicResource SecondaryTextColor}" />
                    </VerticalStackLayout>

                    <HorizontalStackLayout Grid.Column="2" 
                                   VerticalOptions="Center" 
                                   HorizontalOptions="End" 
                                   Spacing="5">
                        <Label Text="{Binding Quantity}"
                       FontSize="16"
                       FontAttributes="Bold"
                       VerticalOptions="End" />

                        <Label Text="{Binding Unit}"
                       FontSize="16"
                       VerticalOptions="End" />
                    </HorizontalStackLayout>

                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>